import { style } from "@vanilla-extract/css";

export const rowBox = style({
  display: "flex",
  alignItems: "center",
});

export const columnBox = style({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});
